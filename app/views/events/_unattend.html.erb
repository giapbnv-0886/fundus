<%= form_for(@event.active_attendances.find_by(user_id: current_user.id), url: event_attendances_path(@event), html:{method: :delete}, remote: true) do |f| %>
  <%= f.submit t("event.view.donotattend") %>
<% end %>
