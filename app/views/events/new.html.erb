<% provide :title, t("event.view.newEvent") %>

<section id="way-help" class="way-help-section">
  <div class="container">
    <div class="row section-content">
     <div class="section-title text-center">
      <div class="section-title-text text-uppercase">
        <h2><%= t "event.view.createEvent" %></h2>
      </div>
    </div>
    <div class="row">
      <div class="col-md-12 form-group">
         <%= render "event_form", event: @event, f_url: cause_events_path(@cause) %>
      </div>
    </div>
  </div>
</div>
</section>

