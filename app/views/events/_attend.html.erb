<%= form_for(Attendance.new, url: event_attendances_path(@event), remote: true) do |f| %>
  <div><%= hidden_field_tag :user_id, current_user.id %></div>
  <%= f.submit t("event.view.attend") %>
<% end %>
