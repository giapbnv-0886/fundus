<% provide :title, t("cadmin.view.attendance.title") %>
<div class="row">

</div>
<div class="row">
  <div class="col-md-6 col-md-offset-3 form-group">
    <%= form_for @attendance do |f| %>
    <div class="field">
      <%= f.label t("cadmin.view.attendance.user_id") %> <br />
      <%= f.text_field :user_id, class: "form-control", disabled: "disabled" %>
    </div>
    <div class="field">
      <%= f.label t("cadmin.view.attendance.event_id") %> <br />
      <%= f.text_field :event_id, class: "form-control", disabled: "disabled" %>
    </div>
    <% end %>
  </div>
</div>

